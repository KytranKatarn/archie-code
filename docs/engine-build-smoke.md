# Engine build-loop smoke

End-to-end proof of the autonomous coordinate->build->test->deploy loop (#4256) running in the provisioned engine container (#4258). Deterministic stub plan; the LLM coder-agent plan wiring is the next slice.
